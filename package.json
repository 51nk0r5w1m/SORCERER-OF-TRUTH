{
  "name": "defcon34",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "npx serve -l 8000 .",
    "build": "echo 'deck is pre-built — nothing to compile'",
    "test": "node --test tests/deck.test.mjs && npx playwright test",
    "test:static": "node --test tests/deck.test.mjs",
    "test:e2e": "npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
