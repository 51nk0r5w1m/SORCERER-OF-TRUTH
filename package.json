{
  "name": "defcon34",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "start": "npx serve -l 8000 .",
    "build": "echo 'deck is pre-built; nothing to compile'",
    "test": "node --test tests/deck.test.mjs && npx playwright test --project=chromium-desktop --project=chromium-mobile",
    "test:static": "node --test tests/deck.test.mjs",
    "test:e2e": "npx playwright test",
    "test:stage": "npx playwright test --project=chromium-stage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
